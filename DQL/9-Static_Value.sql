-- Seleciona um valor fixo (string) 'Hello World!' e dá o nome de coluna "greeting"
-- Para cada linha da tabela customers, essa mesma mensagem será repetida
SELECT
    'Hello World!' AS greeting
FROM customers;
